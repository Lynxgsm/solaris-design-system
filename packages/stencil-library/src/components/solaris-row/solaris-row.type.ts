import { ContainerProps } from '../../types/common/container.props';
import { UnitString } from '../../types/common/unit-string';

export type SolarisRowProps = ContainerProps & {
  gap?: UnitString;
};
