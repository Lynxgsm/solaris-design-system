'use strict';

const unitFormatter = (size = 0, unit = 'px') => (typeof size === 'number' ? `${size}${unit}` : size);

exports.unitFormatter = unitFormatter;

//# sourceMappingURL=unit-4cbd9c03.js.map